@*@model I_highlights *@

<div class="dc-highlights">
    <div class="row">
        <div class="col-12 col-md">@await Html.PartialAsync("_Highlight")</div>
        <div class="col-12 col-md">@await Html.PartialAsync("_Highlight")</div>
        <div class="col-12 col-md">@await Html.PartialAsync("_Highlight")</div>
        <div class="col-12 col-md">@await Html.PartialAsync("_Highlight")</div>
    </div>
</div>